from setuptools import setup

setup(
        name='WebFlaskApp',
        packages=['WebFlaskApp'],
        include_package_data=True,
        install_requires=[
            'flask'.
            ],
        )

